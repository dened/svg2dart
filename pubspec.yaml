name: svg2dart
description: A command-line tool to convert SVG files into pure Dart code for Flutter.
version: 0.0.11
homepage: https://github.com/dened/svg2dart 
repository: https://github.com/dened/svg2dart
issue_tracker: https://github.com/dened/svg2dart/issues

executables:
  svg2dart:

topics:
  - svg
  - flutter
  - code-generation
  - widget

screenshots:
  - description: 'Logo of the svg2dart package'
    path: img/logo.png


# This package supports all platforms listed below.
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: '>=3.0.0 <4.0.0'

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  build: ^4.0.2
  dart_style: ^3.1.2
  meta: ^1.16.0
  path: ^1.9.1
  vector_graphics_codec:  ^1.1.13 
  vector_graphics_compiler: ^1.1.19

dev_dependencies:
  build_runner: ^2.10.1 
  build_test: ^3.5.1 
  lints: ^6.0.0 
  test: ^1.26.3
